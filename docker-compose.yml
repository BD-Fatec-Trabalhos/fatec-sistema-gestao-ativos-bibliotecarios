services:
  oracle-xe:
    image: gvenzl/oracle-xe:21-slim
    container_name: oracle_xe_fatec
    ports:
      - "1521:1521"
    environment:
      - ORACLE_PASSWORD=Fatec@2026
      - APP_USER=usr_biblioteca
      - APP_USER_PASSWORD=Fatec@2026
    volumes:
      - oracle_data:/opt/oracle/oradata

volumes:
  oracle_data:
